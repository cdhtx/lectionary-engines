services:
  - type: web
    name: lectionary-engines
    runtime: python
    buildCommand: "pip install -r requirements.txt && pip install -r requirements-web.txt"
    startCommand: "uvicorn web.app:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
